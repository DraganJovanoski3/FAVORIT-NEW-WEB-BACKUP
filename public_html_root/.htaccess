RewriteEngine On

# SPA fallback: send all non-file, non-dir requests to the Angular app at root
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /index.html [L]
